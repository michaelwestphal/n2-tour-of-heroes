/*
import {
  beforeEachProviders,
  describe,
  expect,
  it,
  inject
} from '@angular/core/testing';
import { Ng2CliUpgradeAppComponent } from '../app/ng2-cli-upgrade.component';

beforeEachProviders(() => [Ng2CliUpgradeAppComponent]);

describe('App: Ng2CliUpgrade', () => {
  it('should create the app',
      inject([Ng2CliUpgradeAppComponent], (app: Ng2CliUpgradeAppComponent) => {
    expect(app).toBeTruthy();
  }));

  it('should have as title \'ng2-cli-upgrade works!\'',
      inject([Ng2CliUpgradeAppComponent], (app: Ng2CliUpgradeAppComponent) => {
    expect(app.title).toEqual('ng2-cli-upgrade works!');
  }));
});
*/

/*
import {
  beforeEachProviders,
  describe,
  // ddescribe,
  // expect,
  // iit,
  it,
  inject,
  ComponentFixture,
  TestComponentBuilder
} from '@angular/core/testing';
// import {provide} from 'angular2/core';
import { HeroesComponent } from './heroes.component';

describe('MyApp Component', () => {

  beforeEachProviders((): any[] => []);

  it('should ...', inject([TestComponentBuilder], (tcb: TestComponentBuilder) => {
    return tcb.createAsync(HeroesComponent).then((fixture: ComponentFixture) => {
      fixture.detectChanges();
    });
  }));

});
*/
